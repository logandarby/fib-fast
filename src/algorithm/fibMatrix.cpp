#include "fib.h"
#include "util.h"

// struct mat2 {
//     factor_t *a, b, c, d;
// };

std::vector<digit_t> fibonacciMatrix(size_t index) {
    /* M2x2 step = (0, 1, 1, 1); // A B C D
    M2x2 fib(step)
    while (index > 0) {
        if (index & 1) != 0) {
            fib = fib * step;
        }
        step *= step
        index >>= 1;
    }
    return top left
    */
    return std::vector<digit_t>();
}
